“LOG IN” page
If “a”
→ lead to “enter
products” page
Log those
Enter information
products to the
1. Gender “routine”
If “b”
2. Birthday function page
→ lead to “enter
| Create  3.     | Skincare hobbies/routine |                |
| -------------- | ------------------------ | -------------- |
| account using  | a. Fixed routine, fixed  | products” page |
| email/Google/  | products                 |                |
| Apple          | b. Building routine and  |                |
finding products
Recommend
|     | c. Never did skincare  | If “c” →  |
| --- | ---------------------- | --------- |
ingredients
before
prompt user
based on scan
to scan first
results

“HOME” page - first thing they see after opening the app
Directs user to "scan”
User clicks “capture today’s scan”
page
User sees the
most recent
scan score
and subscores
of each
category. Two
buttons below
Directs user to "routine”
User clicks “log routine”
page
If a scan has been submitted today already, the “capture today’s scan” will be blurred out. User
can only click “log routine”. User can only submit one scan per day

Screen
changes
“SCAN” page
back to
scanning
function
If user doesn’t like
the scan, he can
press “retake” button
at the bottom
The scanning page disappears,
replaced by a “score report” page.
Criteria include redness, blemish,
Back to “HOME”
User
hydration, color. Below the score
screen. Show the
scans
report, include a small line graph
decrease/increase
that shows the score trend up
of score since last
If user likes the
until today.
scan. If it is
scan, he can
increase, use green
click "finalize”
font and show “+__”;
The app should remember every scan captured. if decrease, use
Store every scan in a database. There should only grey font and show
be one scan per day. “-___”.

“ROUTINE” page
User checks off  Clicks  Shows “routine saved”, automatically
| AM/PM routine | “submit” | return to home screen |
| ------------- | -------- | --------------------- |
User CANNOT go back and check/uncheck routine after hitting submit.

“INGREDIENTS” page
User scans product
Results
label or manually
come out
input the ingredient

“PROGRESS” page
If “weekly”, show a
time lapse video of
7-consecutive
pictures starting at
the start of the week
User sees big line
User selects
graph that has “days”
“monthly” or “weekly”
on x axis, “score” on
timelapse.
y axis.
If “weekly”, show a
time lapse video of
30-consecutive
pictures starting at
the start of the month