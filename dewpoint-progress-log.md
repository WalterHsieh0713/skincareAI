---
name: dewpoint-progress-log
description: Running log of Dewpoint development sessions and what was accomplished
metadata: 
  node_type: memory
  type: project
  originSessionId: 1034e6d6-6a3c-4b3a-a9aa-592022f4d181
---

Running progress log for the Dewpoint skincare app.

**2026-07-26 (merged teammate's login/onboarding rebuild, more scan-quality fixes, glass-surface bugfixes; iOS layout bug still open):**
- **Pulled 3 commits from `origin/main`** (`ce7e9bf`, `ca95ffc`, plus a log/hook-fix commit): a new login/onboarding flow (`src/app/login.tsx`, `src/app/onboarding/{profile,products,first-scan}.tsx`, replacing the earlier `onboarding/welcome.tsx`), a profile store (`src/lib/profile*.ts`, `hooks/use-profile.ts`), an OCR native-bridge webview (`ocr-webview-host(.web).tsx`, `lib/ocr-native-bridge.ts`), and a score-trend chart (`components/score-trend-line.tsx`) — already merged against the Cherry Blossom theme on the teammate's end. Clean fast-forward, no code conflicts. `npm install` pulled in ~19 new packages (`tesseract.js` for OCR among them); dev server relaunched.
- **Face-detection thresholds loosened again (3rd pass):** scans still weren't validating after the 2026-07-16 loosening. Widened further in both `scan-calibration.ts`/`.web.ts`: `MIN_FACE_FILL` 0.1→0.05, `NO_FACE_FILL` 0.02→0.01, `MAX_CENTER_OFFSET` 0.26→0.35, `MIN_EVENNESS` 0.22→0.12, `isSkin()` chrominance band widened again (nr 0.33–0.50→0.30–0.53, ng 0.24–0.44→0.21–0.47, saturation floor 0.02→0.01). Not yet confirmed against a real device — flag if still rejecting.
- **Texture score was pinning to a hard 0:** `scan-image.ts`/`.web.ts` computed texture as a raw per-pixel luminance gradient normalized through a hard linear clamp (`1 - norm(texture, 3, 22)`) — ordinary sensor noise/JPEG artifacts routinely pushed the raw gradient past 22, so real photos scored a flat 0 regardless of actual skin texture. Fixed with a 3×3 box blur (`boxBlur3`) before measuring the gradient, then retuned the linear `norm()` bounds to `(4, 34)` for the new post-blur scale (an exponential-falloff attempt was tried first but came out steeper than the other three metrics' linear scale — texture read ~50 while the others read ~80s on the same shot — and was reverted). **Bounds are still a best-guess, not calibrated against real capture data.**
- **Stale score card after "Clear History":** `scan.tsx` stays mounted across tab switches, and its score/quality/prevScore state lives in `useScanCapture()` (per-component `useState`), decoupled from the shared scan store — clearing history on Progress emptied the store but left the old score card showing on Scan. Fixed with a `useEffect` in `use-scan-capture.ts` that resets local state whenever `scans.length` hits 0.
- **Fixed a stale automation:** the `PostToolUse` hook in `.claude/settings.json` (auto-commits/pushes this progress log on every save) still hardcoded the pre-move path `c:/Users/sean/Desktop/Claude/Dewpoint`, missed by the 2026-07-15 repo-move cleanup — silently failing since then. Updated to `c:/Users/user/dev/skincareAI`.
- **Fixed a real rendering bug from the glass-card redesign:** `Card`'s shadow used RN's iOS-style `shadow*` props, which Metro was explicitly warning about ("shadow* style props are deprecated. Use boxShadow") — on web these render as a hard-edged pink outline instead of a soft blur, showing up as a "weird border" around every card. Fixed with `Platform.select`: real CSS `boxShadow` on web, kept native `shadow*`/`elevation` for iOS/Android.
- **Fixed a second, related bug** (glass `Card` is translucent now, not opaque): several screens nested a `ThemedView type="backgroundElement"` (solid) directly inside a `Card` purely for layout grouping — harmless when `Card` was flat, but now renders as a jarring opaque box-within-glass-box. Found and fixed 4 instances: `scan.tsx`, `routine.tsx`, and two in `onboarding/first-scan.tsx` — swapped to plain `View`.
- **Fixed a `Screen.tsx` double-safe-area-inset bug:** `contentInset={insets}` was set unconditionally on the `ScrollView`, but `contentPlatformStyle` padding (and, on native, `AppHeader` rendered as a real sibling above it) already applied the same top/bottom insets — doubling both. Removed `contentInset` entirely; per-platform `contentPlatformStyle` padding is now the single source of truth.
- **iOS Expo Go bug reported, partially diagnosed, NOT yet confirmed fixed:** Sean saw app content confined to the bottom ~half of the screen with a black area above it (not cropped — shifted down). Added `height: '100%'` alongside `flex: 1` on `Screen`'s root `View` — `NativeTabs` (`expo-router/unstable-native-tabs`, still `unstable`) hosts each tab's screen in a native container that doesn't reliably establish a JS flex context, so `flex: 1` alone can leave content sized-to-fit and bottom-pinned inside a taller native view, exposing the OS window background (black in iOS dark mode) above it. Matches the existing defensive `{flex:1, height:'100%'}` pattern already used in `app-tabs.web.tsx`. **Not verified on-device — no simulator/device access from this environment.**
- **Separately found the dev server was serving a stale Metro cache** after a mid-session process restart that skipped `--clear`: Sean's phone kept showing the already-deleted `welcome.tsx` screen (fabricated preview scores matched exactly) even after force-quitting Expo Go and rescanning. Restarted the tunnel dev server with `--clear` (log confirmed "Bundler cache is empty, rebuilding"). **Whether this actually resolves the black-bar-on-iOS layout issue is still unconfirmed** — follow up next session if Sean reports it's still broken.
- **Verification:** `tsc --noEmit` clean throughout. Dev server (tunnel mode, port 8081) confirmed bundling cleanly for web. No real-device re-test yet for the loosened thresholds, the texture rescale, or the iOS layout fix — next session should confirm all three against an actual phone.

**2026-07-16 (UI redesign — Cherry Blossom theme + welcome onboarding screen; separate track from the same-day native-tab-bar session below):**
- Built a first-launch welcome/preview screen (`src/app/onboarding/welcome.tsx`, since removed — see 2026-07-26 above) shown before the routine-builder wizard: an app-preview card (reusing `SkinScoreView` with fabricated demo scores) filling most of the screen, headline + "Get Started" CTA. No backend exists, so Get Started continued straight into the existing routine-builder flow rather than collecting real signup info.
- Iterated through several full theme passes with Sean against Figma color-combination references, landing on "Cherry blossom" (`#F2C7C7`/`#FFFFFF`/`#D5F3D8`/`#FFB7C5`): rice-white/cream background (retuned several times — `#FCF4E9` → `#FBEDE5` → `#FBF3E1`, etc.), pink CTA accent, green swapped out of `backgroundSelected` entirely (tried light purple, then whitish-yellow, then grey/"metallic silver" per live feedback), a "glassy" translucent-surface system via a new `GlassView` component (`expo-blur` `BlurView` + tint + soft white glint border), and a soft top-down `Glow` gradient background (replaced an earlier full diagonal gradient wash after Sean asked to simplify to flat color, then asked for the glow back).
- Added a gradient "Dewpoint" wordmark (`GradientText` component). Found that `@react-native-masked-view/masked-view`'s web implementation is a no-op stub that silently drops the gradient and renders flat black text (confirmed by reading its source) — added `gradient-text.web.tsx`, a platform override using real CSS `background-clip: text` for web, keeping the native `MaskedView`+`LinearGradient` version for iOS/Android. Iterated colors/direction per feedback (black→pink→white horizontal → purple→grey→black vertical → purple→black vertical, for light mode; white→peach→pink horizontal for dark mode, since a black bookend vanished into the dark background).
- New Fraunces (serif — headlines/wordmark/hero numbers) + Manrope (sans — body/UI) type pairing via `@expo-google-fonts/*`, loaded through `useFonts` in the root layout. Fixed a real bug along the way: the wordmark's `p` descender was getting clipped on web — `background-clip: text` needs a taller line-height/box than the glyph's natural leading, or the browser cuts off descenders.
- Set `DEFAULT_SETTINGS.themePreference` to `'light'` (was `'system'`) so the app doesn't default into dark mode mid-theme-review.
- Built on branch `feature/welcome-onboarding-screen` per Sean's request (kept `main` untouched during active theme experimentation), later merged into `main`. One real conflict in `src/components/ui/screen.tsx` — both this branch and the same-day native-tab-bar fix (below) restructured the same `return` statement. Resolved by hand, keeping both: the native `AppHeader`-in-`Screen` + scroll-to-top fix, and this branch's `Glow` gradient background. Pushed to `origin/main` as `8eb2dec`.

**2026-07-16 (first real-phone test via Expo Go — native tab bar, camera mirror, face-detection tuning):**
- **Native bottom tab bar was completely missing:** `app-tabs.tsx` wrapped `NativeTabs` (from `expo-router/unstable-native-tabs`, backed by a real native `UITabBarController`/Compose tab bar via `react-native-screens`) inside a plain `<View><AppHeader/><NativeTabs>`. That wrapper — added 2026-06-30 — had never actually been run on a physical device before today (all prior verification was web/Playwright only), and it turns out `NativeTabs` needs to own its screen directly; nesting it as a sibling under another view silently dropped the tab bar chrome entirely (content just filled the screen). Fix: `AppTabs` (native) now returns `NativeTabs` directly with nothing wrapping it.
- **Reintroduced the Dewpoint header for native:** since `NativeTabs` can no longer host a sibling `AppHeader` above it, `AppHeader` now renders per-screen instead, inside `src/components/ui/screen.tsx` (`Platform.OS !== 'web' && isTabScreen && <AppHeader/>`, gated on `useSegments()[0] === '(tabs)'` so it doesn't also show up on Settings/routine-editor pages, which already have their own native Stack header). Web is unaffected — `app-tabs.web.tsx` still renders `AppHeader` once above the tab pill, unchanged.
- **Scroll position now resets on tab focus:** `Screen` added a `useFocusEffect` (`@react-navigation/native`) that scrolls its `ScrollView` back to `y: 0` (unanimated) every time the screen regains focus — previously switching tabs and back left you scrolled to wherever you'd left off.
- **Front camera mirror fixed (native):** `camera-capture.tsx` was faking the mirror with a plain RN `transform: scaleX(-1)` style on `CameraView`, which doesn't reliably flip the actual native camera surface — the live preview on a phone never visually mirrored, unlike the web version (which mirrors correctly via canvas). Fixed by using `expo-camera`'s own `mirror` prop on `CameraView`, which flips both the live preview and the saved photo at the native/hardware level on iOS and Android; removed the now-redundant manual post-capture `ImageManipulator.flip('horizontal')`.
- **Post-capture rejection feedback made more prominent:** `scan.tsx`'s "checking..." spinner and the retake/issue-list card used to render as separate `Card`s below the camera; now both render inside the same `Card` as `CameraCapture`, directly under the Retake button, so the feedback reads as attached to the capture action instead of detached further down the screen. (Considered building true pre-capture live guidance for native to match web's ~350ms polling, but `expo-camera` has no cheap raw-frame-sampling API — only `takePictureAsync`, which would mean an actual still capture every poll — so Sean chose to keep native's existing photo-first flow and just improve its visibility instead.)
- **Face-detection thresholds loosened (still not fully resolved):** Sean reported real faces getting rejected as "no face detected" and "uneven lighting" too often on his phone. Loosened, identically in both `scan-calibration.ts` and `scan-calibration.web.ts`: `isSkin()`'s normalized chrominance band (nr 0.36–0.47 → 0.33–0.50, ng 0.28–0.40 → 0.24–0.44, saturation floor 0.04 → 0.02 — still tone-invariant, not reverting to absolute brightness floors), `NO_FACE_FILL` (0.04 → 0.02), and `MIN_EVENNESS` (0.35 → 0.22). **After this pass, Sean still hit "no face detected" on his phone** — follow-up for next session; may need actual device photos to debug rather than further blind threshold tuning (lighting conditions, camera exposure/white-balance differences between his phone and the dev/test environment, or the `WORK=256`px analysis resolution losing too much detail, are all worth checking before loosening further).

**2026-07-10 (post-scan recommendations):**
- **Post-scan ingredient/product-type recommendations (Feature 2 → 4 bridge):** built by the `junior-app-developer` subagent, committed as `188ae1c`. After each scan (and on Progress for the latest scan), axes scoring below `RECOMMENDATION_THRESHOLD = 70` get brand-neutral ingredient suggestions + generic product types (blemishes → salicylic acid/benzoyl peroxide/etc.; redness → cica/azelaic; texture → AHAs/retinoids; hydration → HA/ceramides), with a link to the Ingredient Checker and a not-medical-advice disclaimer; all-good scans show a positive state. New: `src/constants/recommendations.ts`, `src/lib/recommendations.ts`, `src/components/skin-recommendations.tsx`, shared `src/components/ingredient-row.tsx` (extracted from ingredients.tsx). Added Benzoyl Peroxide (new `Antimicrobial` category) to the INCI DB. Strings in all 9 catalogs. Follow-ups: the Ingredient Checker link doesn't prefill recommended ingredients (no router-params pattern yet under typedRoutes); per-axis threshold tuning once real scan data exists.

**2026-06-30 (GitHub setup + ingredient screen + live scan gating):**
- **GitHub setup:** installed/authenticated GitHub CLI (`gh`, account `longsianglee`). Pushed the full Dewpoint folder to the shared repo `WalterHsieh0713/skincareAI` (merged with the existing placeholder README rather than overwriting it). Added a PostToolUse hook (`.claude/settings.json`) so every future save to this progress log is automatically committed and pushed to `origin/main` — no manual push needed going forward.
- **Ingredient category search (Feature 4):** `matchIngredients()` in `src/lib/match-ingredients.ts` now recognizes when the entire input is a category name (e.g. "AHA", case-insensitive, exact match) and returns every ingredient in that category, reusing the existing "Paste a list instead" box and Key actives/Base & support results UI — no new UI needed. Verified live: typing "AHA" returns Glycolic/Lactic/Mandelic/Malic/Tartaric Acid (actives) + Citric Acid (base).
- **Colloquial ingredient descriptions:** rewrote the `function` text for all 82 ingredients in `src/constants/ingredients.ts` from full descriptive sentences to short, plain-language phrases (e.g. Adapalene: "OTC prescription-strength retinoid for acne." → "Alleviates acne"). Category chip badge (already existing, previously unnoticed) stays as-is — description text no longer duplicates the category name. English catalog only; other 7 language catalogs not yet updated for either this or the category-search placeholder hint.
- **Live pre-capture gating for the guided selfie scan (Feature 1):** Sean captured a badly-framed selfie (face far above the guide ring) and it still got scored, because `calibrateScan()`'s validation only ran *after* capture. Extracted the existing metrics/validation logic (`detectFace`, `laplacianVariance`, `evennessFrom`, `validate`) into a shared `assessFrame()`/`assessVideoFrame()` in `scan-calibration.web.ts`, and added a `liveGuide` prop to `CameraCapture` (`camera-capture.web.tsx`) that polls the live video feed every ~350ms, disables the Capture button, and shows coaching text (reusing existing `scan.guidance.*` i18n strings) until framing/lighting pass. Kept the app's monochrome visual language (no red/green — text + disabled-button state only, per Sean's call). Scoped to Feature 1 only via the prop (Feature 4's label camera unaffected — verified). Native camera stub gets matching prop types only (still a non-functional placeholder, unchanged behavior).
- **Auto-score on capture:** removed the extra "Score this scan" tap — added an `autoConfirm` prop to `CameraCapture` so pressing Capture on the Scan tab now calls `onCapture` immediately instead of requiring a second confirmation press. Kept "Retake" as the only action shown post-capture (escape hatch if the auto-scored shot isn't wanted). Feature 4's ingredient-label camera keeps its original Retake/"Use this scan" two-step flow (didn't set the new prop).
- **Verification method note:** no real camera hardware in this dev sandbox, so the live-gating and auto-score features were verified by monkey-patching `getUserMedia` with a canvas-based synthetic video stream (a movable skin-toned circle) driven via gstack browse — confirmed the Capture button genuinely disables/enables (`[disabled]` in the accessibility tree) as the synthetic face moved on/off-center, and that capture flows straight to a saved score. Sean should still spot-check on a real device.

**2026-06-27:**
- Got the app running in a browser for the first time — started the Expo web dev server (`npx expo start --web`) in `C:\Users\sean\Desktop\Claude\Dewpoint` and opened it in Chrome at `http://localhost:8081`. (First guess of dewpoint.app was wrong — it's a local dev server only.) See [[dewpoint-launch-local]] for the launch steps.
- Moved the memory files out of Claude Code's hidden built-in memory folder (`.claude\projects\...\memory\`) into the project root: `MEMORY.md`, `dewpoint-launch-local.md`, `dewpoint-progress-log.md`.
- Wired `MEMORY.md` into `CLAUDE.md` (added `@MEMORY.md` import + a Session Startup instruction) so it auto-loads at the start of every session.
- Mapped the codebase: app code lives in `src/`, entry point is `package.json` → `expo-router/entry` → `src/app/_layout.tsx` (root) → `src/app/index.tsx` (home). Full structure recorded in [[MEMORY]].

**2026-06-27 → 06-28 (feature session):**
- **AM/PM routine on device time zone (Feature 3):** added `src/lib/time.ts` (`localTimeZone()` via `Intl`, `currentPart()` noon cutoff). Routine screen now detects the active part from the iPhone's local zone, orders the active routine first, highlights it, and shows the detected zone (e.g. `PM now · America/New_York`). Day boundaries already used local-time getters, so streaks were already zone-correct — this added the time-of-day awareness. Noon cutoff is `PM_CUTOFF_HOUR` in `time.ts` if Sean wants it changed.
- **CV calibration + validation for every scan (Feature 1 moat):** added `src/lib/scan-calibration.web.ts` (+ fail-closed native stub). Pipeline = capture → validate → calibrate → score → save. Validates face presence/framing (skin-region localization), exposure, lighting evenness (quadrant luminance), and focus (variance-of-Laplacian); calibrates via gray-world white balance + exposure normalization (gains clamped ≤1.8×). Invalid captures are rejected with coaching and never scored/saved. `ScanQuality` stored on each `Scan`. `detectFace()` is the documented seam to drop in MediaPipe FaceLandmarker later. Thresholds are named constants at the top of the file.
- **Settings (toolbar button + page):** restructured nav to a root `Stack` wrapping a URL-transparent `(tabs)` group (all tab files moved to `src/app/(tabs)/`, routes unchanged) plus `src/app/settings/*`. Added a ⚙ Settings button to the `Screen` top-row toolbar. Settings hub → Account, Subscription, Preferences (System/Light/Dark, live theme switch via `useResolvedColorScheme`), Language, Privacy & Security (change username/password), Terms. Persisted via `src/lib/settings-store(.web).ts` + `hooks/use-settings.ts`. **Account/password actions and the subscription toggle are demo-only — no backend yet.**
- **Terms & Conditions doc generated:** `src/constants/legal.ts` (rendered in-app) + standalone `legal/terms-and-conditions.md`. 16 sections tailored to Dewpoint (not-medical-advice, BIPA/biometric, auto-renew subscription, liability, etc.). **It's a template, not legal advice — needs counsel review before release.** Per CLAUDE.md, output files belong in Sean's Google Drive *Dewpoint* folder — NOT yet uploaded.
- **Full app localization (i18n):** added `src/i18n/` — `catalogs/*` with English as source of truth + 8 full curated translations (es, fr, de, pt-BR, it, ja, ko, zh-Hans; pt-PT→pt-BR, zh-Hant→zh-Hans; ~50 other picker languages fall back to English). `translate(tag,key,params)` + `hooks/use-translation.ts` (`t`/`tn`) bound to `settings.language`, so changing Settings → Language re-renders the whole app instantly. Every user-facing string across all screens/tab bars/headers/camera now routes through `t()`. T&C legal body stays English by design (picker labels each language "Fully translated" vs "Falls back to English"). RULE: never hardcode UI text — add a catalog key. Arabic/Hebrew etc. are not in the curated set and would also need RTL layout work.
- **Verification each step:** `tsc --noEmit` clean; all 12 routes return 200 and bundle without errors. Note: moving the tab files into `(tabs)/` once corrupted Expo's incremental typed-routes cache (emitted `/index` instead of `/`); fixed with an `expo start --clear` restart. Dev server currently runs as a background task on `localhost:8081`.

**2026-07-01 (merged Walter's changes + live-scan-gating fix):**
- **Merged teammate's push:** Walter (`WalterHsieh0713`) pushed 2 commits to `origin/main` (ingredient category search, colloquial ingredient copy, live scan gating, auto-score capture) while local had uncommitted work. Committed local changes as a WIP checkpoint first, then pulled. 5 files overlapped; 2 auto-merged cleanly (`scan.tsx`, `en.ts`), 3 had real conflicts (`camera-capture.web.tsx`, `scan-calibration.ts`, `scan-calibration.web.ts`) because both sides had independently built the same live-scan-quality-check feature with different implementations (local: `assessQuality`/`ISSUE_PRIORITY`/`WORK` export + canvas-ref pattern; Walter's: `assessFrame`/`assessVideoFrame` + `captureBlocked`/`liveMessage` gating). Resolved by taking Walter's version entirely in those 3 files, per Sean's call.
- **Post-merge break found + fixed:** `tsc --noEmit` caught that `scan.tsx` still imports `ISSUE_PRIORITY` from `@/lib/scan-calibration`, but Walter's merged version only defined it as an unexported local constant (and the native stub didn't have it at all) — would have crashed at runtime the first time a scan failed validation. Fix: exported `ISSUE_PRIORITY` from `scan-calibration.web.ts` and added the matching stub export to `scan-calibration.ts` (native), no logic changes. `tsc --noEmit` clean after.
- **Verified in-browser:** installed Playwright + Chromium locally, started the dev server on port 8082 (8081 was already occupied), drove `/scan` with a fake camera feed (`--use-fake-device-for-media-stream`). Confirmed live gating shows "No face detected" coaching text and disables the Capture button correctly, with zero console/page errors. Couldn't exercise the "valid face → scored" happy path — headless Chromium's synthetic camera feed isn't a real face.
- Git identity configured repo-locally (`Walter` / `dynamic.walter@gmail.com`) since this machine had none set.

**2026-07-06 (scan-driven streaks + customizable routine):**
- **Streak decoupled from routine checklist:** `computeStreak`/`repairableDay`/`canRestore`/`restoresRemaining` in `src/lib/routine.ts` now take a `ScanDaySet` (set of scan `dayKey`s) instead of the AM/PM `RoutineLog` — a day counts toward the streak only if a scan was saved that day (or a restore patched it). `useRoutine()` builds the scan-day set from `useScans()`. The AM/PM `RoutineLog`/adherence tracking still exists and still renders on the Routine tab, but it no longer feeds the streak — Home/Routine copy updated to say so (`home.scannedToday`/`noScanToday`, `routine.streakHint`). Verified live: manually marking both AM and PM done for today left the streak at 0; injecting a scan for today bumped it to 1.
- **User-customizable AM/PM routine (Feature 3):** replaced the hardcoded 4-step AM/PM lists with a user-authored routine. New `src/lib/routine-config.ts` + `routine-config-store(.web).ts` (native in-memory / web `localStorage` under `dewpoint-routine-config`) hold `{ config: { am: string[], pm: string[] }, onboarded: boolean }`. Shared `src/components/routine-builder.tsx` form (add/remove steps per section) is used by both `src/app/onboarding/routine.tsx` (first-launch wizard, with a "Skip for now" escape hatch) and `src/app/routine-editor.tsx` (pushed from a new ✎ edit icon on each Morning/Evening card on the Routine tab).
- **First-launch gating:** `(tabs)/_layout.tsx` redirects to `/onboarding/routine` via `<Redirect>` whenever `onboarded` is false, so a fresh install (or cleared `dewpoint-routine-config`) always hits the routine builder before the tab bar. No real auth/registration exists yet (per Sean's call — first-launch detection stands in for "when they register an account" until a real signup flow is built).
- **Verified live via gstack browse:** confirmed the onboarding redirect fires on first load, filled in AM ("Gentle cleanser") and PM ("SPF 50") steps and saved, landed back on Home with the new steps showing on the Routine tab; opened the ✎ editor, added "Vitamin C serum" to AM, saved, confirmed it persisted. Confirmed via localStorage/IndexedDB injection that the streak ignores routine-checklist submissions and responds only to scans. `tsc --noEmit` clean. No new i18n keys were added to the other 8 language catalogs (English only) — they fall back to English per the existing i18n design; flagged to Sean as a follow-up if full localization is wanted.
- **Pushed to GitHub:** committed and pushed directly to `WalterHsieh0713/skincareAI` `main` (commit `7a7d6d5`) — no PR, per Sean's direct request to push the changes.

**2026-06-29 (streak loss + restore + time-lapse gating):**
- **Streak loss already correct:** `computeStreak` (in `src/lib/routine.ts`) already resets to 0 after one fully-missed past day; an in-progress *today* stays in grace so it doesn't break early. No change to the break rule — the work was the restore mechanic and the time-lapse coupling.
- **Streak restores (2/month):** reworked the routine store to hold `RoutineState = { log, restored }` where `restored` maps a patched day key → epoch ms the restore was spent. `restoreStreak()` bridges the single day that broke the streak (`repairableDay`, Duolingo-style — only a 1-day gap with a counted day just before it is bridgeable; consecutive gaps are patched one restore at a time). `MAX_RESTORES_PER_MONTH = 2`, rationed by calendar month via `restoresUsedThisMonth`/`restoresRemaining`. Web store migrates the legacy bare-`RoutineLog` localStorage shape into `{ log, restored }`.
- **Time-lapse gated by streak:** `isRangeUnlocked(range, streak)` = `streak >= RANGE_DAYS[range]` (weekly 7 / biweekly 14 / monthly 30 / 3-month 90). Matches Sean's spec: miss on day 5 (streak 4) → no weekly; miss on day 10 (streak 9, today's grace) → weekly yes, biweekly no. `progress.tsx` dims locked range buttons (🔒) and shows a "unlocks at an N-day streak — you're at {streak}" hint instead of the player.
- **UI:** added an `action` slot to `Card` (top-right of the title row). Home screen's Routine-streak card now has a compact **Restore streak** button top-right (dimmed when no restore is available — no credits or no bridgeable gap) plus an "{n} restores left this month" line. New i18n keys in `en.ts` (`home.restoreStreak`, `home.restoresLeft_one/_other`, `progress.rangeLocked`, `progress.rangeLockedHint`); other catalogs fall back to English.
- **Verification:** `tsc --noEmit` clean. (`expo lint` couldn't run — `eslint` not installed in `node_modules`, pre-existing env issue.)
