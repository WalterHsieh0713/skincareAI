# Expo HAS CHANGED

The project runs Expo SDK 54 (downgraded from 56 for native camera support — see git history). Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.
